server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
            search-locations: classpath:/shared-config/
#        git:
#          uri: https://github.com/wzelechowski/healthy-config-server.git
#          clone-on-start: true
#          default-label: main

